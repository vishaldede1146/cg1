--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.35.0
onnxruntime==1.20.1
numpy==2.1.3
pandas==2.2.3
scikit-learn==1.6.1
joblib==1.5.3
shap==0.52.0
huggingface_hub==0.27.1
uvicorn
fastapi
python-multipart

# tensorflow-cpu / tf2onnx are NOT needed here anymore -- churn_predictor
# now serves via onnxruntime (see products/churn_predictor/churn_src/export_onnx.py).
# Only install tensorflow-cpu + tf2onnx locally when re-exporting the model
# after retraining.


